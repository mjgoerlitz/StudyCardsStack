// create back side of card, answers

// unimplemented
import React from 'react'
import { API_URL } from "./config";
import { TDeck } from './getDecks'

/*export async function createBackSide(
  deckId: string,
  index: number
): Promise<TDeck> {
  const response = await fetch(`${API_URL}/decks/${deckId}/cards/${index}`, {
    method: "POST", // not sure what to add here
  });
  return response.json();
}*/